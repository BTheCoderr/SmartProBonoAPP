# SmartProBono Standalone App - Minimal Dependencies
# Only the essential packages needed for the standalone app

# Core Flask dependencies
flask==3.0.0
flask-cors==4.0.0
python-dotenv==1.0.0
gunicorn==21.2.0

# HTTP requests for Supabase
requests==2.31.0

# Note: smtplib, datetime, json, re, os, logging are built-in Python modules
