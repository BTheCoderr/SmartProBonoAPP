# SmartProBono FastAPI App - Python 3.13 Compatible
# Minimal dependencies that work with Python 3.13

# Core FastAPI dependencies
fastapi==0.104.1
uvicorn[standard]==0.24.0
python-multipart==0.0.6
pydantic==2.5.0
python-dotenv==1.0.1
requests==2.31.0

# Database (Supabase)
psycopg2-binary==2.9.9

# Authentication
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4

# Basic utilities
python-dateutil==2.8.2
tenacity==8.2.3

# Production server
gunicorn==21.2.0

# Note: Removed problematic packages:
# - Pillow (image processing not needed for core API)
# - pymongo (using Supabase instead)
# - motor (using Supabase instead)
# - redis (not needed for basic functionality)
# - All Flask dependencies (using FastAPI)
# - Testing dependencies (not needed in production)
# - Development tools (not needed in production)
# - PDF generation tools (can be added later if needed)
# - Analytics tools (can be added later if needed)
